/* agent-42 TUI styles — inspired by claudechic */

/* ── Layout ── */

#chat-view {
    height: 1fr;
    padding: 1;
}

#input-container {
    height: auto;
    min-height: 3;
    max-height: 12;
    padding: 1;
}

#input {
    min-height: 1;
    max-height: 10;
}

StatusFooter {
    height: 1;
    dock: bottom;
    background: $surface;
    color: $text-muted;
    padding: 0 1;
}

/* ── Messages ── */

ChatMessage {
    width: 1fr;
    height: auto;
    margin: 0 0 1 0;
}

ChatMessage.user-message {
    border-left: thick $primary;
}

ChatMessage.assistant-message {
    border-left: wide $secondary;
}

ChatMessage MarkdownBlock {
    margin: 0;
}

/* ── Tools ── */

ToolWidget {
    width: 1fr;
    height: auto;
    margin: 0 0 1 0;
    border-left: wide $panel;
    padding: 0 2;
}

ToolWidget .tool-header {
    height: auto;
    width: 1fr;
    color: $text-muted;
}

ToolWidget .tool-header:hover {
    color: $text;
}

ToolWidget .tool-body-text {
    height: auto;
    width: 1fr;
    padding: 0 0 0 2;
    display: none;
}

ToolWidget.expanded .tool-body-text {
    display: block;
}

ToolWidget.pending .tool-header {
    color: $warning;
}

ToolWidget.completed .tool-header {
    color: $text-muted;
}

ToolWidget.error .tool-header {
    color: $error;
}

/* ── Thinking ── */

ThinkingIndicator {
    height: auto;
    padding: 0 1;
    margin: 0 0 1 0;
    color: $text-muted;
}

/* ── Provider Picker ── */

#provider-picker {
    align: center middle;
    width: 1fr;
    height: auto;
}

#provider-picker-box {
    width: 40;
    height: auto;
    border: round $primary;
    padding: 1 2;
}

#provider-picker-box Label {
    width: 100%;
    text-align: center;
    margin: 0 0 1 0;
}

#provider-list {
    width: 100%;
    height: auto;
}

#provider-list ListItem {
    height: 1;
    padding: 0;
}

#provider-list ListItem Label {
    width: 100%;
    text-align: center;
}

/* ── Scrollbar ── */

VerticalScroll>.scrollbar {
    width: 1;
}